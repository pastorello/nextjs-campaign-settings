/** A domain card's type (SPEC-021 §5), stored as `dhDomainCard.type`. */
enum DhDomainCardType {
  Ability = "ability",
  Spell = "spell",
  Grimoire = "grimoire",
}

export default DhDomainCardType;
