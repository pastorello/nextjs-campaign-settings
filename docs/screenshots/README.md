# Screenshots

Images referenced by the root `README.md`. Expected files:

| File              | What to capture                                                           |
| ----------------- | ------------------------------------------------------------------------- |
| `spells-list.png` | `/dashboard/spells` with a few rows, a filter applied and the sort header |
| `spell-form.png`  | `/dashboard/admin/spells/new` — the metadata-driven form                  |
| `world-map.png`   | `/dashboard/geography` — the Leaflet map with a POI open                  |

Keep them under ~500 KB each; the app is not a photo gallery and the repo
should stay quick to clone.
